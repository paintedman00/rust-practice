# Greeter CLI

A simple command-line tool that greets the user.

## Usage

```bash
./greeter_cli <name>
```

Replace `<name>` with the name you want to greet.

## Building

```bash
cargo build --release
```
