# System Health Mock

A simple command-line tool to simulate checking system uptime and other health metrics.

## Usage

Run the program with `cargo run`.

## Functionality

Currently, this tool mocks the system uptime and health status. It's intended as a basic example and can be expanded to include actual system monitoring capabilities.
