# uuid_maker

A simple command-line UUID generator written in Rust.

## Usage

Run the program to generate a UUID:

```bash
cargo run
```

You can also specify the number of UUIDs to generate as an argument:

```bash
cargo run -- 5
```

This will generate 5 UUIDs.
