# uuid_factory

A simple command-line UUID generator written in Rust.

## Usage

```bash
cargo run
```

Generates a single UUID.

```bash
cargo run -- --count 5
```

Generates 5 UUIDs.

## Building

```bash
cargo build --release
```
