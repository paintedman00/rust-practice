# json_struct_lint

A simple command-line tool to validate the structure of JSON data.

## Usage

```bash
json_struct_lint <json_file>
```

The tool reads the provided JSON file and checks if the structure conforms to an expected format. The expected format is defined within the code.

If the JSON structure is valid, the tool outputs "Valid JSON structure". Otherwise, it indicates that the structure is invalid.
