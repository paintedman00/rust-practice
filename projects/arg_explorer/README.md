# Arg Explorer

A simple command-line argument parsing example in Rust.

This project demonstrates how to use the `std::env` module to access and process command-line arguments passed to a Rust program.

## Usage

```bash
cargo run -- [options] <arguments>
```

## Examples

```bash
cargo run -- --help
cargo run -- hello world
cargo run -- -n 5 -v
```
