# text_finder

A simple command-line tool for searching text patterns in files, similar to grep.

## Usage

```bash
text_finder <pattern> <file_path>
```

For example:

```bash
text_finder "hello" my_file.txt
```

This will print lines from `my_file.txt` that contain the word "hello".
