# Hex Inspector

A simple command-line utility to display the hexadecimal representation of a file.

## Usage

```bash
hex_inspector <file_path>
```

## Example

```bash
hex_inspector example.txt
```

This will output the hexadecimal representation of `example.txt` to the console.
