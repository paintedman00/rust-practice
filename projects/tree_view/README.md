# Tree View

A simple command-line tool to visualize directory trees.

## Usage

```bash
cargo run [directory]
```

If no directory is specified, the current directory is used.

## Example

```
$ cargo run .
.
├── src
│   └── main.rs
└── Cargo.toml
```
