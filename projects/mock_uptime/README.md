# Mock Uptime Checker

A simple command-line tool that simulates checking system uptime.

## Usage

Run the program using `cargo run`.

## Output

The program outputs a JSON object indicating the simulated system uptime in seconds.
