# arg_inspector

A simple command-line tool to inspect command-line arguments.

## Usage

Run the program with various arguments to see how they are parsed and displayed.

```bash
cargo run -- --help
cargo run -- arg1 arg2 arg3
cargo run -- --option1 value1 --option2 value2
```
