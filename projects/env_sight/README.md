# EnvSight

A simple command-line tool to inspect environment variables.

## Usage

Run `env_sight` to display all environment variables.

```bash
cargo run
```

You can also filter by a prefix:

```bash
cargo run -- prefix
```

Replace `prefix` with the desired prefix to filter the environment variables.

## Building

Make sure you have Rust installed. Then:

```bash
cargo build
```
