# Permissions Inspector

A simple command-line tool to analyze file permissions.

## Usage

```bash
cargo run <file_path>
```

Replace `<file_path>` with the path to the file you want to inspect.  The tool will output permission information.
