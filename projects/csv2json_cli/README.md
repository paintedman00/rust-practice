# csv2json_cli

A simple command-line tool to convert CSV data to JSON format.

## Usage

```bash
csv2json_cli input.csv output.json
```

This will read the CSV data from `input.csv` and write the JSON output to `output.json`.

## Building

```bash
cargo build --release
```

The executable will be located in `target/release/csv2json_cli`.
