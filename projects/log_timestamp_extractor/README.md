# Log Timestamp Extractor

A simple command-line tool to extract timestamps from log files.

## Usage

```bash
cargo run -- <log_file_path>
```

This will output the extracted timestamps to the console.
